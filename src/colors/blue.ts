import type { Color } from '../types';
import { brand } from '../utils';

export const BLUE: Color = brand({
	rawHex: 0x0000ff,
	hex: '#0000ff',
	r: 0,
	g: 0,
	b: 255,
	rgb: {
		r: 0,
		g: 0,
		b: 255
	}
});
