import type { Color } from '../types';
import { brand } from '../utils';

export const BLACK: Color = brand({
	rawHex: 0x000000,
	hex: '#000000',
	r: 0,
	g: 0,
	b: 0,
	rgb: {
		r: 0,
		g: 0,
		b: 0
	}
});
