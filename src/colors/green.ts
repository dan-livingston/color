import type { Color } from '../types';
import { brand } from '../utils';

export const GREEN: Color = brand({
	rawHex: 0x00ff00,
	hex: '#00ff00',
	r: 0,
	g: 255,
	b: 0,
	rgb: {
		r: 0,
		g: 255,
		b: 0
	}
});
