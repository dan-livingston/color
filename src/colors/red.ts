import type { Color } from '../types';
import { brand } from '../utils';

export const RED: Color = brand({
	rawHex: 0xff0000,
	hex: '#ff0000',
	r: 255,
	g: 0,
	b: 0,
	rgb: {
		r: 255,
		g: 0,
		b: 0
	}
});
